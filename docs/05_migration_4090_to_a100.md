# 05 Migration 4090 To A100

从本地 4090 迁移到集群 A100 时，优先检查以下差异：

1. 路径是否从本地磁盘切换为 NVMe 工作目录。
2. CUDA、PyTorch、vLLM 版本是否与目标节点兼容。
3. 模型是否已经提前离线同步到计算节点。
4. 启动参数是否考虑 GPU 显存和并行度差异。
5. 是否改为通过 `slurm/` 中的脚本提交任务。

建议先在本地跑 `smoke_test`，再到 A100 跑同名实验，减少变量数量。
